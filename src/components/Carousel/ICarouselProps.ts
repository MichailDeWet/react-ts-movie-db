import ICategorizedMovie from "@api/ICategorizedMovie";

/**
 * Carousel takes these props.
 *
 * @export
 * @interface ICarouselProps
 */
export default interface ICarouselProps {
  input: ICategorizedMovie;
}
