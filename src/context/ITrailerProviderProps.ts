import { ReactNode } from "react";

/**
 * Allows the trailer to accept children of type ReactNode as a prop.
 *
 * @export
 * @interface ITrailerProviderProps
 */
export default interface ITrailerProviderProps {
  children: ReactNode;
}
